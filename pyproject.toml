[project]
name = "rembg-service"
version = "0.1.0"
description = "A Flask service for future AI-based image background removal"
requires-python = ">=3.11"
dependencies = [
    "flask==3.0.3",
    "flask-cors==5.0.0",
]

[tool.uv]
dev-dependencies = []